타입 스크립트 사용이유

1.  컴파일 단계에서의 에러 발견
2. 자동완성을 통한 개발자 경험 개선
3. 문서화

------------

함수타입

** 제네릭타입 ** 

function func(args : number){
    args가 숫자인지 숫자가 아닌지 검사
}

func("5") x
func(5) o

function func<T>(args : T) {
    T를 활용 가능
    args가 숫자인지 숫자가 아닌지 검사
}

=> 타입의 동적 지정을 통함 함수의 재활용

유틸타입